source 'https://rubygems.org'

gem 'rails'
gem 'jquery-rails'
gem 'thin'
gem 'configatron'
<%= "gem 'mongoid'" if mongo? -%>
<%= "gem 'bson_ext'" if mongo? -%>
<%= "gem 'pg'" if postgres? -%>

group :assets do
  gem 'sass-rails'
  gem 'coffee-rails'
  gem 'uglifier'
<%= bootstrap? ? "  gem 'bootstrap-sass'" : "REMOVE" %>
end

group :development, :test do
  gem 'vcr'
  gem 'timecop'
  gem 'factory_girl_rails'
  gem 'guard-rspec'
  gem 'terminal-notifier-guard'
  gem 'rb-fsevent'
  gem 'rspec-rails'
end
